<?php// Settings for this entity:$thisEntity=array(	'ID'=>2, // Your Entity ID in your copy of the Root.Entities table. (e.g. 2 is bank.opentrans.fr)	'Key'=>'', // Your Entity public key, in hex.	'Group'=>1, // Your Entities group ID. (e.g. '1')	'Endpoint'=>'', // Your Entity endpoint (e.g. 'bank.opentrans.fr')	'PrivateKey'=>'' // Your Entity private key, in hex.);// The base path is where the actual API pages are at:$basePath='../private/Pages/';// Is caching disabled?$cachingDisabled=true;// Disable all error reporting://error_reporting(0);// Create the database link (doesn't actually connect until it's used):$dz=new databaseLink('Database Username','Database Password','Database Name');///////////////////////////// If you include the Bank API, you'll need the following settings too.// The main currency this particular bank uses/ prefers:// $bankCurrency='currency.gbp';// Setting this mode enables 'devices' to authenticate too.// $apiMode='bank';// 15 minutes is the lockout duration for invalid requests.// $lockoutDuration=60*15;///////////////////////////?>